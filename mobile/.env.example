# Copy to .env for local development. Do NOT commit .env
# Public mobile configuration only — no server secrets or Admin credentials.

ORA_ENV=development
ORA_API_BASE_URL=https://api-dev.ora.app/v1
ORA_APP_CHECK_ENABLED=false
